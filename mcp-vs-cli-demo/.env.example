# ─── Server URLs ────────────────────────────────────────────────────────────────
# The mock API server (loan applications API)
API_BASE_URL=http://localhost:3000

# The portal static server (serves the loan portal HTML)
PORTAL_URL=http://localhost:3001

# ─── OAuth credentials (same as companion API repo) ────────────────────────────
CLIENT_ID=test-client-id
CLIENT_SECRET=test-client-secret

# ─── Portal test user ────────────────────────────────────────────────────────────
TEST_USER_EMAIL=alex.martinez@example.com
TEST_USER_PASSWORD=TestPass123!

# ─── MCP / Claude API (only needed for tests/mcp — see ABOUT_MCP_TESTS.md) ─────
# ANTHROPIC_API_KEY=sk-ant-...
# MCP_MODEL=claude-3-5-sonnet-20241022

# ─── Environment ─────────────────────────────────────────────────────────────────
NODE_ENV=local
